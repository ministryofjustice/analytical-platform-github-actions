---
name: 🦝 Super-Linter

on:
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  zizmor:
    name: Zizmor
    permissions:
      contents: read
      packages: read
      statuses: write
    uses: ./.github/workflows/reusable-super-linter.yml
